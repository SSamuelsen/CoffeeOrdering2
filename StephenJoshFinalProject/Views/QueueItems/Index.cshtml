@model IEnumerable<StephenJoshFinalProject.Models.QueueItem>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QueueItemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QueueItemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QueueItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
